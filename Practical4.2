#include<iostream>
using namespace std;

   inline int multi(int q, int t){
        return q*t;
        }
        

int main()
{
    int x, y;
    cout<<"enter two values : " ;
    cin>>x>>y;
    int total;
    total=multi(x, y) ;
    
    cout<<total;
    return 0;
}
